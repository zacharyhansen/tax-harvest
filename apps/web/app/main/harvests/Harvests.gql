fragment HarvestTableItem on Harvest {
  id
  date
  type
  step
  createdAt
  amount
  label
  createdBy {
    id
    ...UserItem
  }
}

query Harvests($where: HarvestWhereInput) {
  harvests(where: $where) {
    id
    ...HarvestTableItem
  }
}
