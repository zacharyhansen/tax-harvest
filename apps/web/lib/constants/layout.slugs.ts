/**
 * These should never be updated as the backend uses them as slugs
 */
export enum LayoutSlug {
  DEAL_PAGE = 'DEAL_PAGE',
  TASKS = 'TASKS',
  DEALS = 'DEALS',
  OPPORTUNITIES = 'OPPORTUNITIES',
}
