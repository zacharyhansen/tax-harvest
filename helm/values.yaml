# Global configuration
global:
  namespace: harvester
  environment: production
  domain: dev.taxharvest.ai
  projectID: harvester-ai-428202
  clusterLocation: us-central1-b
  clusterName: harvester-dev
  ingress:
    enabled: true
    annotations:
      kubernetes.io/ingress.global-static-ip-name: harvester-static-ip
      kubernetes.io/ingress.allow-http: 'true'
      kubernetes.io/ingress.class: gce
      networking.gke.io/managed-certificates: harvester-cert
  # Added flag to indicate we're using the global consolidated ingress
  useGlobalIngress: true
# API service configuration
api:
  enabled: true
  replicaCount: 1
  image:
    repository: us-central1-docker.pkg.dev/harvester-ai-428202/harvester-api/harvester-api
    tag: v1.1.3
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 80
    targetPort: 8000
  resources: {}
  # Health check probes
  livenessProbe:
    httpGet:
      path: core/health
      port: http
    initialDelaySeconds: 15
    timeoutSeconds: 5
    failureThreshold: 3
    successThreshold: 1
  readinessProbe:
    httpGet:
      path: core/health
      port: http
    initialDelaySeconds: 15
    timeoutSeconds: 3
    failureThreshold: 3
    successThreshold: 1
  ingress:
    path: /core
    pathType: Prefix
  # Service account for GCP workload identity
  serviceAccountName: google-secret-service-account
  # Cloud SQL Auth Proxy configuration
  cloudsql:
    enabled: true
    image:
      repository: gcr.io/cloud-sql-connectors/cloud-sql-proxy
      tag: latest
      pullPolicy: IfNotPresent
    instanceConnectionName: 'harvester-ai-428202:us-central1:harvester-dev'
    usePrivateIp: true
    resources: {}
  env:
    NODE_ENV: production
    extras:
      DB_HOST: '/cloudsql/harvester-ai-428202:us-central1:harvester-dev'
      PORT: 8000
      LOG_LEVEL: info
      ORIGIN: 'https://dev.taxharvest.ai'
      PLAID_ENV: 'https://production.plaid.com'
      CLIENT_ORIGIN: 'http://dev.taxharvest.ai'
      CLIENT_HOME_PAGE_PATH: /main/home
      CRON: true
      ETRADE_OAUTH_AUTHORIZE_URL: 'https://us.etrade.com/e/t/etws/authorize'
      ETRADE_HOSTNAME: api.etrade.com
      GOOGLE_PROJECT: harvester-ai-428202
      SECRET_SOURCE: GCP
      GCS_LOTS_BUCKET_NAME: lot_upload
    # Add a new section for secret references
    secretRefs:
      - name: DATABASE_URL
        valueFrom:
          secretKeyRef:
            name: database # Created by ExternalSecret
            key: DATABASE_URL
      - name: CLERK_SECRET_KEY
        valueFrom:
          secretKeyRef:
            name: clerk
            key: CLERK_SECRET_KEY
  migrations:
    enabled: false # Set to false to skip running migrations
    image:
      repository: us-central1-docker.pkg.dev/harvester-ai-428202/harvester-api/harvester-api
      tag: v1.0.3
      containerCommand: [/bin/sh, -c]
      containerArgs: ['chmod +x ./scripts/migrate.sh && ./scripts/migrate.sh']
    # Migration-specific environment variables
    env:
      extras:
        # Add migration-specific environment variables here
        MIGRATION_MODE: apply
        LOG_LEVEL: debug
    # Migration-specific secrets
    secretRefs:
      - name: DATABASE_URL
        valueFrom:
          secretKeyRef:
            name: database # Created by ExternalSecret
            key: DATABASE_URL
    # Ensure Cloud SQL configuration is explicitly added for migrations
    cloudsql:
      enabled: true
      instanceConnectionName: 'harvester-ai-428202:us-central1:harvester-dev'
      usePrivateIp: true
# Web app configuration
web:
  enabled: true
  replicaCount: 1
  image:
    repository: us-central1-docker.pkg.dev/harvester-ai-428202/harvester-web/harvester-web
    tag: v1.1.3
    pullPolicy: Always
  service:
    type: ClusterIP
    port: 80
    targetPort: 3000
  resources: {}
  # Health check probes for the web frontend
  livenessProbe:
    httpGet:
      path: /api/health # Next.js specific health endpoint
      port: http
    initialDelaySeconds: 30
    periodSeconds: 10
    timeoutSeconds: 3
    failureThreshold: 3
    successThreshold: 1
  readinessProbe:
    httpGet:
      path: /api/health # Next.js specific health endpoint
      port: http
    initialDelaySeconds: 15
    periodSeconds: 5
    timeoutSeconds: 2
    failureThreshold: 2
    successThreshold: 1
  ingress:
    path: /
    pathType: Prefix
  # Service account for GCP workload identity
  serviceAccountName: google-secret-service-account
  env:
    NODE_ENV: production
    extras:
    # Frontend configuration - next public env's need to be added to the deployment env fiels so they are created at build time
    # Add more environment variables as needed
    # Add a new section for secret references to match the API structure
    secretRefs:
      - name: CLERK_SECRET_KEY
        valueFrom:
          secretKeyRef:
            name: clerk
            key: CLERK_SECRET_KEY
      - name: STRIPE_SECRET_KEY
        valueFrom:
          secretKeyRef:
            name: stripe
            key: STRIPE_SECRET_KEY
# Secrets configuration with External Secrets Operator
# secrets:
#   enabled: true
# The rest of the configuration comes from the secrets chart's values.yaml
